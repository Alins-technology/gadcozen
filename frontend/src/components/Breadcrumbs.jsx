// Breadcrumb trail turned off site-wide (kept as a no-op component instead of
// deleting it from every page, so it's a one-line change to bring back later).
export default function Breadcrumbs() {
  return null;
}
